# Example Workflow

```bash
python3 ../src/ad_fake_account_detector.py --input security_events.jsonl --out reports/
```

Review generated files:

```text
reports/<timestamp>/summary.csv
reports/<timestamp>/findings.json
reports/<timestamp>/report.html
```
